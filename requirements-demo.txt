# Requirements for the public Render demo deployment (demo_server.py)
# sentence-transformers and PyTorch are intentionally excluded.
# The public demo uses GeminiEmbedder (Google API) which has zero local model RAM footprint.
# Local desktop users should install from requirements.txt instead, which includes sentence-transformers.
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
chromadb>=0.4.22
python-frontmatter>=1.0.0
google-genai>=0.1.1
requests>=2.28.0
python-dotenv>=1.0.0
rank-bm25>=0.2.2
