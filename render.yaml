services:
  - type: web
    name: local-iq-demo
    env: python
    buildCommand: pip install -r requirements-demo.txt
    startCommand: gunicorn --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT demo_server:app
    envVars:
      - key: EMBEDDING_PROVIDER
        value: gemini
